#include<iostream>
using namespace std;
int main()
{
	int m,n;
	cout<<"enter the two numbers";
	cin>>n>>m;
	{
		if(m==n)
		cout<<"the given number is equal";
		else
		cout<<"the given number is not equal";
		return 0;
	}
}
